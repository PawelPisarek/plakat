{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Przed list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nazwaobrazka</th>
                <th>
                    obrazek
                </th>
                <th>grupa</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>


        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('przed_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nazwaObrazka }}</td>
                <td> <img src="{{ asset(entity.getUploadMinaturkaDir~'/' ~ entity.nazwaObrazka  ) }}"/></td>
                <td>{{ entity.grupa}}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('przed_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('przed_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('przed_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
