{% extends '::base.html.twig' %}

{% block body -%}
    <h1>typy list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('typy_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('typy_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('typy_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>


    {% for ent in entities %}

        {{ ent.atrybuty }} <br/>
    {% endfor %}

        <ul>
        <li>
            <a href="{{ path('typy_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
