{% extends '::base.html.twig' %}

{% block body -%}
    <h1>adres</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Miasto</th>
                <td>{{ entity.miasto }}</td>
            </tr>
            <tr>
                <th>Ulica</th>
                <td>{{ entity.ulica }}</td>
            </tr>
            <tr>
                <th>Kodpocztowy</th>
                <td>{{ entity.kodpocztowy }}</td>
            </tr>
            <tr>
                <th>Nrtelefonu</th>
                <td>{{ entity.nrTelefonu }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ entity.email }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('adres') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('adres_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
