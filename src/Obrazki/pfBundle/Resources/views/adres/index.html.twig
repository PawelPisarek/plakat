{% extends '::base.html.twig' %}

{% block body -%}
    <h1>adres list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Miasto</th>
                <th>Ulica</th>
                <th>Kodpocztowy</th>
                <th>Nrtelefonu</th>
                <th>Email</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('adres_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.miasto }}</td>
                <td>{{ entity.ulica }}</td>
                <td>{{ entity.kodpocztowy }}</td>
                <td>{{ entity.nrTelefonu }}</td>
                <td>{{ entity.email }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('adres_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('adres_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('adres_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
