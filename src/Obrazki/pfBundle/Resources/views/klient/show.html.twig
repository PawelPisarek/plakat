{% extends '::base.html.twig' %}

{% block body -%}
    <h1>klient</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Login</th>
                <td>{{ entity.login }}</td>
            </tr>
            <tr>
                <th>Haslo</th>
                <td>{{ entity.haslo }}</td>
            </tr>
        </tbody>
    </table>


    {% for zam in entity.zamowienia %}
        {{ zam }}
    {% endfor %}

        <ul class="record_actions">
    <li>
        <a href="{{ path('klient') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('klient_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
