{% extends '::base.html.twig' %}

{% block body -%}
    <h1>zamowienie list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Datazlozenia</th>
                <th>Zaplacono</th>
                <th>Datawysylki</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('zamowienie_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.dataZlozenia %}{{ entity.dataZlozenia|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.zaplacono }}</td>
                <td>{% if entity.dataWysylki %}{{ entity.dataWysylki|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('zamowienie_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('zamowienie_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('zamowienie_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
